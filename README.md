# Blue Bot Cartesian Robot
Work in progress...


## Python Requirements
Create a new environment
```python -m venv /path/to/new/virtual/environment```

Install all 
```pip install -r requirements.txt```
